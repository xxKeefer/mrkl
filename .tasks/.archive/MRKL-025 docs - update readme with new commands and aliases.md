---
id: MRKL-025
type: docs
status: in-progress
created: '2026-03-02'
---

## Description

Update README.md to document the `close` and `prune` commands, add a command alias reference table,
show option aliases for `create` and `list`, document interactive `create` mode, and add alias
examples to Quick Start.

## Acceptance Criteria

- [x] Alias reference table listing all 7 commands with their short aliases
- [x] Quick Start section includes alias usage examples
- [x] `close` command section with positional arg and behaviour
- [x] `prune` command section with positional arg, `--force` option, and behaviour
- [x] `create` option table shows `-d` and `-a` aliases
- [x] `list` option table shows `-t` and `-s` aliases
- [x] Interactive mode note under `create` command
